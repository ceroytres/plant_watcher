defaults:
  - lux_model: estimated
app:
  name: "Plant Watcher"
  colors:
    background: "#00b34b"
    text: "#FEFFFF"

server:
  host: 0.0.0.0
  port: 5000
  debug: false

browser:
  url: http://${server.host}:${server.port}/
  open: false

serial:
  port: "/dev/ttyACM0"
  baudrate: 9600
